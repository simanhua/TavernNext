## Agent skills

### Issue tracker

Issues and specs are tracked in GitHub Issues for `simanhua/TavernNext`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels with a one-to-one name mapping. See `docs/agents/triage-labels.md`.

### Domain docs

Use the single-context layout: root `CONTEXT.md` plus `docs/adr/`, created lazily when needed. See `docs/agents/domain.md`.
